<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/authFragment">

    <fragment
        android:id="@+id/authFragment"
        android:name="ru.cepprice.githubprojects.ui.fragment.AuthFragment"
        android:label="fragment_auth"
        tools:layout="@layout/fragment_auth" >
        <action
            android:id="@+id/action_authFragment_to_reposListFragment"
            app:destination="@id/reposListFragment" />
    </fragment>
    <fragment
        android:id="@+id/reposListFragment"
        android:name="ru.cepprice.githubprojects.ui.fragment.ReposListFragment"
        android:label="fragment_repos_list"
        tools:layout="@layout/fragment_repos_list" >
        <argument
            android:name="accessToken"
            app:argType="string" />
    </fragment>
</navigation>